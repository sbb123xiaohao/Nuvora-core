# 项目修改与交付约定

用户要求每轮都使用已经包含此前全部修改的最新完整版本。

1. 修改前先核对 GitHub `main` 的最新提交与本地完整文件树；如远端已有新改动，先同步，再开始工作。不能以旧压缩包或残缺目录覆盖新版本。
2. 完成修改后先运行适用的构建和测试，再提交；提交后重新核对远端最新提交。
3. 每轮都从**最新完整提交**重新生成完整项目压缩包，包含该提交全部跟踪文件。向用户发送完整包的下载链接；补丁、差异文件或仅修改过的文件不能代替完整包。
4. 打包后逐一核对压缩包内文件与最新提交的内容、路径及数量一致。编译缓存和本地生成的数据盘镜像不属于源码包；若用户要求构建产物，应明确另行交付。
